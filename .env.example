# This is an example .env file for local development.
# 1. Rename this file to .env OR create a new file named .env
# 2. Place it in the root of your ELIZA_App project (same directory as requirements.txt).
# 3. Add your actual database connection string.
# Important: DO NOT commit your actual .env file to version control if this is a public repository.

DATABASE_URL='postgresql://YOUR_NEON_DB_USER:YOUR_NEON_DB_PASSWORD@YOUR_NEON_DB_HOST_AND_POOLER/YOUR_NEON_DB_NAME?sslmode=require'
