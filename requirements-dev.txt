# Test-only dependencies. NEVER installed in production (Vercel installs
# only requirements.txt) - this file exists so pytest/faker never ship in the
# deployed app.
#
# Pinned against what actually resolves for the Python/Flask/SQLAlchemy
# versions already pinned in requirements.txt (Flask 3.1.3, Flask-SQLAlchemy
# 3.1.1, SQLAlchemy 2.0.52, Flask-WTF 1.3.0, Werkzeug 3.1.8, Python 3.13.3
# in venv/ as of this writing) - see README.md "Running the tests".
pytest==9.1.1
pytest-cov==7.1.0
faker==40.38.0
