# Environment / secrets
.env
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# Local DB / logs
*.db
*.log
uploads/

# Testing
.pytest_cache/
.coverage
htmlcov/

# OS/editor
.DS_Store
.vscode/
.idea/

# Local debugging screenshots (not part of the app)
/isssues/

# One-off migration safety snapshots - these are raw dumps of live production
# data (real customer names/emails/phones/addresses) taken before a schema
# change, never application code. Keep them locally if useful, never commit -
# git history is forever, and this repo may not always stay private.
migration_backup_*.json
.git_commit_msg.txt
